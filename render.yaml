services:
  - type: web
    name: website-demo-api
    runtime: python
    plan: free
    rootDir: flask-api
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11

  - type: web
    name: website-demo
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: PREDICT_API_URL
        fromService:
          type: web
          name: website-demo-api
          envVarKey: RENDER_EXTERNAL_URL
      - key: PREDICT_API_PATH
        value: /predict
